# Dependencies
node_modules/
bun.lock

# Build output
dist/
*.node

# Rust build artifacts
addon/target/
addon/*.node

# Driver build artifacts
driver/build/
driver/x64/
driver/ARM64/
driver/*.sys
driver/*.pdb
driver/*.cat
driver/*.cer
driver/.vs/

# IDE
.vs/
.vscode/
*.suo
*.user

# OS
Thumbs.db
Desktop.ini

# Env
.env
.env.*
driver/ghostcom/
driver/src_driver_pp.i

# Allure — raw results and generated HTML are regenerable; trend data
# under allure-history/ is persisted on the `allure-history` orphan
# branch (via scripts/publish-allure-history.ts). The worktree for that
# branch lives in .allure-history-wt/.
allure-results/
allure-report/
allure-history/
.allure-history-wt/
.allure-history-init-*/
